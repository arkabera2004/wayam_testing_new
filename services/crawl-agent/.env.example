# Which browser-use runtime executes crawls. See README.md#agent-backend-config.
CRAWL_AGENT_BACKEND=self_hosted

# LLM behind the agent's reasoning (self_hosted backend only).
GOOGLE_API_KEY=

# Reserved for the browser-use Cloud Agent API — not yet wired (see README).
BROWSER_USE_CLOUD_API_KEY=
