# Provisioned by the Vercel Marketplace (Neon). Pull with: vercel env pull .env.local --yes
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Clerk — provisioned on the project, not yet wired into the app.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Overrides the tenant used by the seed script and the auth stub.
SEED_USER_ID=demo-user
